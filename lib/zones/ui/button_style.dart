import 'dart:ui';

import 'package:flutter/material.dart';

class ButtonStyle extends StatelessWidget {
  const ButtonStyle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(

      ),
    );
  }
}
